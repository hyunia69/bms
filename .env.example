# Copy to .env and fill in. .env is gitignored — never commit secrets.

# OpenAI — embeddings (text-embedding-3-small, 1536d).
# Get a key: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Supabase (bms-poc project). Dashboard → Project Settings → API.
SUPABASE_URL=https://pqwttqyhusoyaxoonlpo.supabase.co
# service_role key (NOT anon) — bypasses RLS, keep secret.
# Throwaway POC project, so low-stakes; still don't commit it.
SUPABASE_SERVICE_ROLE_KEY=
